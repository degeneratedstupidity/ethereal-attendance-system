[pytest]
# Run all async tests automatically without @pytest.mark.asyncio on every function.
asyncio_mode = auto

# Discover tests in the tests/ directory.
testpaths = tests

# Show individual test names and short tracebacks for clarity.
addopts = -v --tb=short
